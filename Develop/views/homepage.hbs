{{#if blogPosts.length}}
    <h2>Latest Blog Posts</h2>
    <ul>
        {{#each blogPosts}}
            <li>
                <h3>{{title}}</h3>
                <p>{{content}}</p>
            </li>
        {{/each}}
    </ul>
{{else}}
    <p>No blog posts available.</p>
{{/if}}